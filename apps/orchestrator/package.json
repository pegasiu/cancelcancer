{
  "name": "@cancelcancer/orchestrator",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@cancelcancer/db": "workspace:*",
    "@cancelcancer/shared": "workspace:*",
    "hono": "^4",
    "bullmq": "^5",
    "ioredis": "^5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
